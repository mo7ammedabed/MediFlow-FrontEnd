// Shared JavaScript functionality

document.addEventListener('DOMContentLoaded', () => {
  // We can add sidebar toggling logic if needed in the future
  // For now, it stays fully expanded on desktop as per the designs
});
